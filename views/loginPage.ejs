<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="flex justify-center">
    <div class="lg:w-[600px] md:w-[500px] sm:w-[400px] xs:w-[380px] w-[340px] py-6 md:py-6 lg:py-12">
        <form class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4" action="/user/login" method="post">
            <h2 class="text-3xl font-bold text-blue-500 mb-4">Scatch</h2>
            <div class="mb-4">
                <div class="block text-gray-700 text-sm font-bold mb-2">Email</div>
                <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" name="email" type="email" placeholder="Enter Email">
            </div>
            <div class="mb-6">
                <div class="block text-gray-700 text-sm font-bold mb-2">Password</div>
                <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 mb-3 leading-tight focus:outline-none focus:shadow-outline" name="password" type="password" placeholder="Enter Password">
            </div>
            <div class="flex items-center justify-between">
                <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">Login</button>
            </div>
        </form>
        <form action="/user/createAccountPage" method="get" class="text-right px-8 relative bottom-[72px]">
            <button class="font-bold text-sm text-blue-500 hover:text-blue-800" type="submit">Create Account?</button>
        </form>
        <% if(error.length > 0) {%>
            <div class="text-center text-red-600 text-xs">
                <%= error[0] %>
            </div>
        <% } %>
        <p class="text-center text-gray-500 text-xs absolute w-full bottom-4 right-0">
            &copy;2024 Scatch Corp. All rights reserved.
        </p>
    </div>
</body>
</html>