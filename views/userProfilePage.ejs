<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="bg-gray-100 flex flex-col items-center min-h-screen">

    <!-- Navbar -->
    <nav class="flex justify-between items-center bg-white w-full xl:h-24 lg:h-20 md:h-16 sm:h-14 h-12 shadow-md top-0 left-0 xl:px-16 lg:px-14 md:px-12 sm:px-10 px-6 z-50">
        <form action="/" method="get">
            <button class="font-bold text-blue-500 lg:text-4xl md:text-3xl sm:text-2xl text-xl">Scatch</button>
        </form>
        <div class="flex items-center space-x-4">
            <form action="/user/cart" method="get">
                <button class="text-red-500 hover:text-red-700 font-bold md:px-4 px-2 lg:text-base sm:text-sm text-xs">Cart</button>
            </form>
            <form action="/user/orders" method="get">
                <button class="text-green-500 hover:text-green-700 font-bold md:px-4 px-2 lg:text-base sm:text-sm text-xs">Orders</button>
            </form>
        </div>
    </nav> 

    <!-- Profile Container -->
    <main class="flex flex-col items-center w-full flex-grow px-4 pt-24 pb-8">
        <div class="w-full max-w-md bg-white shadow-lg rounded-lg p-6 transform transition duration-300 hover:scale-105">
            <!-- User Image -->
            <div class="flex justify-center mb-6">
                <% if (user.image) { %>
                    <img src="data:image/png;base64,<%= user.image.toString('base64') %>" alt="User Image" class="w-24 h-24 sm:w-28 sm:h-28 md:w-32 md:h-32 rounded-full object-cover shadow-md transition duration-300 hover:shadow-xl">
                <% } else { %>
                    <img src="/images/default.png" alt="User Image" class="w-24 h-24 sm:w-28 sm:h-28 md:w-32 md:h-32 rounded-full object-cover shadow-md transition duration-300 hover:shadow-xl">
                <% } %>
            </div>

            <!-- User Info -->
            <div class="text-center mb-8">
                <h2 class="text-2xl font-semibold text-gray-800"><%= user.fullName %></h2>
                <p class="text-gray-500"><%= user.email %></p>
            </div>

            <div class="space-y-4">
                <!-- Contact Number -->
                <div class="flex items-center justify-between">
                    <span class="text-gray-700 font-semibold">Contact:</span>
                    <span class="text-gray-800"><%= user.contact || 'Not Provided' %></span>
                </div>
                
                <!-- Address -->
                <div class="flex items-center justify-between">
                    <span class="text-gray-700 font-semibold">Address:</span>
                    <span class="text-gray-800"><%= user.address || 'Not Provided' %></span>
                </div>
            </div>

            <!-- Edit Button -->
            <div class="mt-6 text-center">
                <a href="/user/editProfile" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded-full transition duration-300 transform hover:scale-105">Edit Profile</a>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="w-full text-center text-gray-500 text-xs py-4">
        &copy;2024 Scatch Corp. All rights reserved.
    </footer>
</body>
</html>
